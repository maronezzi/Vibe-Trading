.venv/
agent/.venv/
__pycache__/
.pytest_cache/
*.pyc
.ruff_cache/

# Coverage reports
.coverage
coverage.xml

# Node dependencies
node_modules/

# Backtest output artifacts
results/

# Environment files
.env
agent/.env

# IDE
.claude/*
!.claude/hooks.json
CLAUDE.md
.vscode/

# External repos
learn-claude-code-main/

# Agent runtime
agent/sessions/
agent/runs/
agent/.ui_runtime/
agent/.tasks/
agent/uploads/
agent/tests/*_result.json
agent/.pytest_tmp/
agent/vibe_trading.egg-info/
agent/vibe_trading_ai.egg-info/
agent/tests/e2e_backtest/
agent/tests/e2e_*.py
agent/tests/test_e2e_*.py
agent/venv/

# Frontend
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Wiki
wiki/node_modules/
wiki/dist/
wiki/.astro/
wiki/.vite/
wiki/.wrangler/
wiki/.cache/

# Build artifacts
dist/
build/
.wrangler/
.cache/
.vibe-dev/
.vibe-record/
.remotion/

# DuckDB data cache
data/*.duckdb
data/*.duckdb.wal
data/.duckdb_tmp/
data/parquet/
data/backup/
data/minutes/
data/overnight*.log
data/*.log
data/test_write.txt

# Internal docs — ignore infra/transient content under docs/ but track
# engineering markdown (plan, gap analyses, proposals) so plan recovery
# is via git, not on-disk backups alone. Wave 19.1.
docs/*
!docs/*.md
!docs/**/*.md
!docs/*/
!wiki/docs/
!wiki/docs/**

# Local demo videos
assets/*.mp4

# Local demo workspace
demo/

# Working notes
NOTES.md
AGENTS.md

# macOS
.DS_Store

# Alpha Zoo wiki
wiki/alpha-library/manifest.json
wiki/alpha-library/content/

# Reports (generated, not source)
reports/

# Backups
*.bak*
*.backup_*
vt_config.json.bak*
*.db.bak*

# Temporary/result files
*.xlsx

# Database (runtime data, not source)
vt_trades.db
journal.db
trade_log.db

# Archive project venvs/node_modules (large, not source)
archive/agent_project/venv/
archive/prediction_project/.venv/
archive/prediction_project/saved_models/
archive/prediction_project/results/
archive/frontend_project/node_modules/

# Hermes Agent working dir (plan files, scratch, etc. — local to this machine)
.hermes/

# Snapshot dirs created by legacy test-fix scripts (commit footgun:
# a single `git add -A` would commit 361 files. Wave 19.7).
tests.snapshot_*/
tests.snapshot_*

# Timestamped validation reports from data-validation runs (not source).
data/validation_3days_*.md

# Snapshots AGI (backup runtime, vão pra vault, não git)
vt_config.json.snapshot_*

# ZCode workspace (workflow separado, não parte do VT)
.zcode/

# Archive docs (sepultura)
archive/docs/
